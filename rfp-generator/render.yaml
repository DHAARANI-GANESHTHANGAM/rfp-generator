services:
  - type: web
    name: ProposAI-backend
    runtime: python
    rootDir: rfp-generator/backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port 10000
    envVars:
      - key: PORT
        value: 10000